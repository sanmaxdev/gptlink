interface:
  display_name: "GPTLink Autonomous Images"
  short_description: "Install, authenticate, manage, and create images"
  default_prompt: "Use $gptlink-image to set up GPTLink autonomously, reuse my existing Codex authentication when possible, and generate an image."
