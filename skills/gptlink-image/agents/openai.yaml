interface:
  display_name: "GPTLink Images"
  short_description: "Operate GPTLink through its enabled plugin"
  default_prompt: "Use $gptlink-image with the GPTLink plugin tools to set up the gateway and generate an image."
