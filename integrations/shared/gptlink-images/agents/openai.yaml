interface:
  display_name: "GPTLink Images"
  short_description: "Generate and edit images through GPTLink"
  default_prompt: "Use $gptlink-images to create or edit the requested image and return the saved image path."
dependencies:
  tools:
    - type: "mcp"
      value: "gptlink"
      description: "GPTLink image generation and editing server"
